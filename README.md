# LegacyFixer subdirectory requirements test

Controlled repository for validating LegacyFixer behavior when Python dependencies are declared outside the repository root.

Expected structure:

- no root requirements.txt
- backend/requirements.txt contains a vulnerable dependency
